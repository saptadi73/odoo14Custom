<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <act_window id="action_create_bill_val1" name="Create Bill"
            res_model="medical.appointments.bill.wizard" binding_model="medical.appointment"
            view_mode="form" target="new" />

        <record id="medical_appointments_bill_wizard_view" model="ir.ui.view">
            <field name="name">medical.appointments.bill.wizard.view</field>
            <field name="model">medical.appointments.bill.wizard</field>
            <field name="arch" type="xml">
                <form string="Create Bill">
                    <h2>
                        Do you want to create the bill(s) ?
                    </h2>
                    <footer>
                        <button name="create_bill" string="Create Bill"
                            type="object" class="oe_highlight" />
                        or
                        <button string="Cancel" type="object"
                            special="cancel" class="oe_highlight" />
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_medical_appointments_bill_wizard" model="ir.actions.act_window">
            <field name="name">Create Bill</field>
            <field name="res_model">medical.appointments.bill.wizard</field>
            <field name="binding_view_types">form</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

</odoo>
